[bar/top]
background            ="${color.background}"
foreground            ="${color.foreground}"
font-0                ="${font.primary}"
font-1                ="${font.symbol}"
height                ="${size.height}"
modules-left          ="menu clear temperature backlight alsa cpu memory"
modules-right         ="battery network date"
border-color          ="${color.border}"
border-bottom-size    ="${size.border}"

[bar/bottom]
background            ="${color.background}"
foreground            ="${color.foreground}"
font-0                ="${font.primary}"
font-1                ="${font.symbol}"
height                ="${size.height}"
modules-left          ="play stop pause title"
modules-right         ="suspend close reboot poweroff"
bottom                ="true"
border-color          ="${color.border}"
border-top-size       ="${size.border}"
